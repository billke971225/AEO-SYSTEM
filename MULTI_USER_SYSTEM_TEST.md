# 多用户认证系统测试指南

## 🧪 **测试完成状态**

✅ **AI图片生成器用户认证集成** - 完成
✅ **API密钥管理器** - 完成  
✅ **前端认证组件** - 完成
✅ **数据库架构** - 完成
✅ **安全策略** - 完成

---

## 🔧 **快速测试步骤**

### **1. 访问多用户界面**

```
http://localhost:5173/multi-user
```

### **2. 用户注册测试**

1. 点击"注册"标签
2. 填写测试信息：
   - 姓名：测试用户
   - 公司：测试公司
   - 邮箱：test@example.com
   - 密码：test123456

### **3. API密钥配置测试**

1. 登录后进入"API密钥"标签
2. 添加测试API密钥：
   - OpenAI: `sk-test123...`
   - Gemini: `AIza...`

### **4. 图片生成测试**

```javascript
// 测试API调用
const testImageGeneration = async () => {
  const response = await fetch('/functions/v1/ai-image-generator', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${userToken}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      topic: '测试主题',
      image_suggestions: [
        {
          type: 'chart',
          description: '测试图表',
          alt_text: '测试图表',
          caption: '这是一个测试图表',
          creation_priority: 'high'
        }
      ],
      generation_method: 'template_based',
      quality_level: 'standard'
    })
  });
  
  const result = await response.json();
  console.log('生成结果:', result);
};
```

---

## 🔍 **功能验证清单**

### **用户认证**
- [ ] 用户注册功能
- [ ] 用户登录功能  
- [ ] 用户登出功能
- [ ] JWT令牌验证

### **API密钥管理**
- [ ] 添加API密钥
- [ ] 查看API密钥列表
- [ ] 删除API密钥
- [ ] 密钥加密存储

### **数据隔离**
- [ ] 用户只能看到自己的API密钥
- [ ] 用户只能访问自己的生成内容
- [ ] RLS策略正常工作

### **AI图片生成**
- [ ] 用户认证验证
- [ ] API密钥自动获取
- [ ] 使用统计记录
- [ ] 成本追踪

---

## 🐛 **常见问题排查**

### **认证失败**
```bash
# 检查Supabase配置
echo $VITE_SUPABASE_URL
echo $VITE_SUPABASE_ANON_KEY
```

### **API密钥解密失败**
```bash
# 检查加密密钥
echo $ENCRYPTION_SECRET
# 确保长度至少32字符
```

### **数据库连接问题**
```sql
-- 检查用户表
SELECT * FROM auth.users LIMIT 5;

-- 检查API密钥表
SELECT user_id, api_provider, is_active FROM user_api_keys;
```

---

## 📊 **测试数据示例**

### **测试用户**
```json
{
  "email": "test@example.com",
  "password": "test123456",
  "full_name": "测试用户",
  "company_name": "测试公司"
}
```

### **测试API密钥**
```json
{
  "provider": "openai",
  "api_key": "sk-test123456789",
  "key_name": "测试OpenAI密钥"
}
```

### **测试图片生成请求**
```json
{
  "topic": "人工智能发展",
  "image_suggestions": [
    {
      "type": "infographic",
      "description": "AI发展时间线",
      "alt_text": "人工智能发展历程图",
      "caption": "展示AI技术发展的重要节点",
      "creation_priority": "high"
    }
  ],
  "generation_method": "gemini_nanebanaen",
  "quality_level": "high"
}
```

---

## ✅ **成功标准**

1. **用户可以成功注册和登录**
2. **API密钥可以安全存储和检索**
3. **图片生成功能正常工作**
4. **数据完全隔离（用户A看不到用户B的数据）**
5. **使用统计正确记录**
6. **成本追踪准确**

---

## 🎉 **测试完成**

如果所有测试都通过，说明多用户认证系统已经成功集成！

现在每个用户都可以：
- 安全地管理自己的API密钥
- 生成专属的AI图片内容
- 查看自己的使用统计
- 享受完全的数据隔离和安全保护